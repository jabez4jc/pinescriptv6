# Pine Script v6 documentation manifest

Generated by `scripts/sync-official-docs.mjs` from the official [User Manual](https://www.tradingview.com/pine-script-docs/welcome/) and [Reference Manual](https://www.tradingview.com/pine-script-reference/v6/). Do not edit by hand.

## How to use

1. Built-in lookup: `ns.name` (e.g. `ta.rsi`, `strategy.entry`, `syminfo.mintick`) is documented in `reference/<ns>.md`. Unnamespaced built-ins: plotting in `reference/plot.md`, time/date in `reference/time.md`, `open`/`close`/`bar_index` etc. in `reference/bar_variables.md`, the rest in `reference/core.md` (or the same-name namespace file, e.g. `input()` in `reference/input.md`). All constants are in `reference/constants.md`.
2. If unsure whether a built-in exists, check `reference/INDEX.md`. Never use a name that isn't listed there.
3. For how and why (execution model, repainting, request.* behavior, strategies), read the matching page under `language/` or `concepts/`.
4. For recent features, check `release_notes.md`. Every script must start with `//@version=6`.

Common routes: execution/var/varip → `language/execution_model.md` · multi-timeframe/request.security → `concepts/other_timeframes_and_data.md` + `concepts/repainting.md` · backtesting → `concepts/strategies.md` + `reference/strategy.md` · drawings → `visuals/lines_and_boxes.md` / `visuals/text_and_shapes.md` · v5 → v6 → `migration_guides/to_pine_version_6.md` · publishing → `writing_scripts/publishing.md` + `writing_scripts/publishing_guidelines.md`.

## Reference manual (signatures, arguments, returns, examples)

Look up exact syntax here before using any built-in. `reference/INDEX.md` lists every built-in name.

- `reference/annotations.md`: **Compiler annotations (//@version, //@function, //@param, //@type, //@field, ...).** 10 entries
- `reference/array.md`: **array.* namespace.** 55 functions, e.g. array.abs(), array.avg(), array.binary_search(), array.binary_search_leftmost(), array.binary_search_rightmost(), array.clear()
- `reference/bar_variables.md`: **Bar data variables (open, high, low, close, volume, bar_index, ...).** 14 variables, e.g. ask, bar_index, bid, close, high, hl2
- `reference/barstate.md`: **barstate.* namespace.** 7 variables, e.g. barstate.isconfirmed, barstate.isfirst, barstate.ishistory, barstate.islast, barstate.islastconfirmedhistory, barstate.isnew
- `reference/box.md`: **box.* namespace.** 30 functions, 1 variable, e.g. box.all, box(), box.copy(), box.delete(), box.get_bottom(), box.get_left()
- `reference/chart.md`: **chart.* namespace.** 5 functions, 11 variables, e.g. chart.bg_color, chart.fg_color, chart.is_heikinashi, chart.is_kagi, chart.is_linebreak, chart.is_pnf
- `reference/color.md`: **color.* namespace.** 8 functions, e.g. color(), color.b(), color.from_gradient(), color.g(), color.new(), color.r()
- `reference/constants.md`: **Built-in constants (color.*, shape.*, plot.style_*, strategy.*, currency.*, ...).** 239 entries
- `reference/core.md`: **Core built-ins (indicator, library, alert, alertcondition, na, nz, fixnan, casts, ...).** 12 functions, 1 variable, e.g. na, alert(), alertcondition(), bool(), fixnan(), float()
- `reference/dividends.md`: **dividends.* namespace.** 3 variables, e.g. dividends.future_amount, dividends.future_ex_date, dividends.future_pay_date
- `reference/earnings.md`: **earnings.* namespace.** 4 variables, e.g. earnings.future_eps, earnings.future_period_end_time, earnings.future_revenue, earnings.future_time
- `reference/footprint.md`: **footprint.* namespace.** 9 functions, e.g. footprint.buy_volume(), footprint.delta(), footprint.get_row_by_price(), footprint.poc(), footprint.rows(), footprint.sell_volume()
- `reference/input.md`: **input.* namespace.** 14 functions, e.g. input(), input.bool(), input.color(), input.enum(), input.float(), input.int()
- `reference/keywords.md`: **Keywords (if, switch, once, for, while, var, varip, type, enum, method, import, export, ...).** 23 entries
- `reference/label.md`: **label.* namespace.** 22 functions, 1 variable, e.g. label.all, label(), label.copy(), label.delete(), label.get_text(), label.get_x()
- `reference/line.md`: **line.* namespace.** 22 functions, 1 variable, e.g. line.all, line(), line.copy(), line.delete(), line.get_price(), line.get_x1()
- `reference/linefill.md`: **linefill.* namespace.** 6 functions, 1 variable, e.g. linefill.all, linefill(), linefill.delete(), linefill.get_line1(), linefill.get_line2(), linefill.new()
- `reference/log.md`: **log.* namespace.** 3 functions, e.g. log.error(), log.info(), log.warning()
- `reference/map.md`: **map.* namespace.** 11 functions, e.g. map.clear(), map.contains(), map.copy(), map.get(), map.keys(), map.new<type,type>()
- `reference/math.md`: **math.* namespace.** 24 functions, e.g. math.abs(), math.acos(), math.asin(), math.atan(), math.avg(), math.ceil()
- `reference/matrix.md`: **matrix.* namespace.** 49 functions, e.g. matrix.add_col(), matrix.add_row(), matrix.avg(), matrix.col(), matrix.columns(), matrix.concat()
- `reference/operators.md`: **Operators (arithmetic, comparison, logical, ternary ?:, history [], assignment :=).** 21 entries
- `reference/plot.md`: **Plotting functions (plot, plotshape, plotchar, fill, hline, bgcolor, barcolor, ...).** 10 functions, e.g. barcolor(), bgcolor(), fill(), hline(), plot(), plotarrow()
- `reference/polyline.md`: **polyline.* namespace.** 2 functions, 1 variable, e.g. polyline.all, polyline.delete(), polyline.new()
- `reference/request.md`: **request.* namespace.** 11 functions, e.g. request.currency_rate(), request.dividends(), request.earnings(), request.economic(), request.financial(), request.footprint()
- `reference/runtime.md`: **runtime.* namespace.** 1 function, e.g. runtime.error()
- `reference/session.md`: **session.* namespace.** 7 variables, e.g. session.isfirstbar, session.isfirstbar_regular, session.islastbar, session.islastbar_regular, session.ismarket, session.ispostmarket
- `reference/str.md`: **str.* namespace.** 18 functions, e.g. str.contains(), str.endswith(), str.format(), str.format_time(), str.length(), str.lower()
- `reference/strategy.md`: **strategy.* namespace.** 48 functions, 35 variables, e.g. strategy.account_currency, strategy.avg_losing_trade, strategy.avg_losing_trade_percent, strategy.avg_trade, strategy.avg_trade_percent, strategy.avg_winning_trade
- `reference/syminfo.md`: **syminfo.* namespace.** 2 functions, 40 variables, e.g. syminfo.basecurrency, syminfo.country, syminfo.currency, syminfo.current_contract, syminfo.description, syminfo.employees
- `reference/ta.md`: **ta.* namespace.** 59 functions, 10 variables, e.g. ta.accdist, ta.iii, ta.nvi, ta.obv, ta.pvi, ta.pvt
- `reference/table.md`: **table.* namespace.** 23 functions, 1 variable, e.g. table.all, table(), table.cell(), table.cell_set_bgcolor(), table.cell_set_height(), table.cell_set_text()
- `reference/ticker.md`: **ticker.* namespace.** 9 functions, e.g. ticker.heikinashi(), ticker.inherit(), ticker.kagi(), ticker.linebreak(), ticker.modify(), ticker.new()
- `reference/time.md`: **Time and date built-ins (time, timestamp, year, month, dayofweek, ...).** 11 functions, 12 variables, e.g. dayofmonth, dayofweek, hour, minute, month, second
- `reference/timeframe.md`: **timeframe.* namespace.** 3 functions, 11 variables, e.g. timeframe.isdaily, timeframe.isdwm, timeframe.isintraday, timeframe.isminutes, timeframe.ismonthly, timeframe.isseconds
- `reference/types.md`: **Types and type qualifiers (int, float, series, simple, array, map, chart.point, footprint, ...).** 20 entries
- `reference/volume_row.md`: **volume_row.* namespace.** 8 functions, e.g. volume_row.buy_volume(), volume_row.delta(), volume_row.down_price(), volume_row.has_buy_imbalance(), volume_row.has_sell_imbalance(), volume_row.sell_volume()

## Primer

Getting started.

- `primer/first_indicator.md`: **First indicator.** The Pine Editor is where you will be working on your scripts.
- `primer/first_steps.md`: **First steps.** Welcome to the Pine Script® v6 User Manual, which will accompany you in your journey to learn to program your own trading tools in Pine Script.
- `primer/next_steps.md`: **Next steps.** After your first steps and your first indicator, let us explore a bit more of the Pine Script® landscape by sharing some pointers to guide you in your journey to learn Pine Script.
- `primer/welcome.md`: **Welcome to Pine Script® v6.** Pine Script® is TradingView’s programming language.
- `primer/where_can_i_get_more_information.md`: **Where can I get more information?.**

## Language

Syntax and semantics: types, execution model, variables, operators, loops, conditionals, functions, UDTs, methods, collections, enums.

- `language/arrays.md`: **Arrays.** Pine Script *arrays* are one-dimensional collections that can store multiple values or references in a single location.
- `language/built_ins.md`: **Built-ins.** Pine Script® has hundreds of *built-in* variables and functions.
- `language/conditional_structures.md`: **Conditional structures.** The conditional structures in Pine Script® are `if`, `switch`, and `once`.
- `language/declaration_statements.md`: **Declaration statements.** In Pine Script®, a *declaration statement* is a mandatory function call that declares the script’s *type* and its *properties* at *compile time*.
- `language/enums.md`: **Enums.** Pine Script Enums, otherwise known as *enumerations*, *enumerated types*, or enum types, are unique data types with all possible values (*members*) explicitly defined by the programmer in advance.
- `language/execution_model.md`: **Execution model.** Pine Script® relies on an event-driven, sequential execution model to control how a script’s compiled source code runs in charts, alerts, Deep Backtesting mode, and the Pine Screener.
- `language/identifiers.md`: **Identifiers.** Identifiers are names used for user-defined variables and functions:
- `language/loops.md`: **Loops.** Loops are structures that repeatedly execute a block of statements based on specified criteria.
- `language/maps.md`: **Maps.** Pine Script *maps* are collections that store data in *key-value pairs*.
- `language/matrices.md`: **Matrices.** Pine Script *matrices* are collections that store values or references in a rectangular format.
- `language/methods.md`: **Methods.** Pine Script methods are specialized functions associated with values of specific built-in types, user-defined types, or enum types.
- `language/objects.md`: **Objects.** Pine Script objects are instances of *user-defined types* (UDTs).
- `language/operators.md`: **Operators.** Some operators are used to build *expressions* returning a result:
- `language/script_structure.md`: **Script structure.** A Pine script follows this general structure:
- `language/type_system.md`: **Type system.** Pine Script® uses a system of *types* and *type qualifiers* to categorize the data in a script and indicate where and how the script can use it.
- `language/user_defined_functions.md`: **User-defined functions.** *User-defined functions* are functions written by programmers, as opposed to the built-in functions provided by Pine Script®.
- `language/variable_declarations.md`: **Variable declarations.** Variables are *named containers* that store calculated values or other data for a script to access and use within a given scope.

## Concepts

Alerts, bar states, inputs, libraries, other timeframes/data (request.*), repainting, sessions, strategies, strings, time.

- `concepts/alerts.md`: **Alerts.** TradingView alerts run 24x7 on our servers and do not require users to be logged in to execute.
- `concepts/bar_states.md`: **Bar states.** A set of built-in variables in the `barstate` namespace allow your script to detect different properties of the bar on which the script is currently executing.
- `concepts/chart_information.md`: **Chart information.** Scripts can retrieve multiple types of information about the current chart and its dataset by using a subset of built-in variables.
- `concepts/common_errors.md`: **Common error messages.** Legacy parser/compiler messages not covered by errors/ (if statement too long, mismatched input, no viable alternative, too many local variables). Hand-maintained.
- `concepts/inputs.md`: **Inputs.** Inputs receive values that users can change from a script's “Settings/Inputs” tab.
- `concepts/libraries.md`: **Libraries.** Pine Script® libraries are publications containing functions that can be reused in indicators, strategies, or in other libraries.
- `concepts/non_standard_charts_data.md`: **Non-standard charts data.** Pine Script® features several `ticker.*()` functions that generate *ticker identifiers* for requesting data from *non-standard* chart feeds.
- `concepts/other_timeframes_and_data.md`: **Other timeframes and data.** Pine Script® allows users to request data from sources and contexts other than those their charts use.
- `concepts/repainting.md`: **Repainting.** We define repainting as: **script behavior causing historical vs realtime calculations or plots to behave differently**.
- `concepts/sessions.md`: **Sessions.** Exchanges define a *session* for every symbol, which represents the times of day and days of the week in which the symbol can be traded.
- `concepts/strategies.md`: **Strategies.** Pine Script® strategies are specialized scripts that simulate trades across historical and realtime bars, allowing users to backtest and forward test their trading systems.
- `concepts/strings.md`: **Strings.** Pine Script® strings are immutable values containing sequences of up to 40,960 encoded characters, such as letters, digits, symbols, spaces, control characters, or other Unicode characters and code points.
- `concepts/time.md`: **Time.** In Pine Script®, the following key aspects apply when working with date and time values:
- `concepts/timeframes.md`: **Timeframes.** The *timeframe* of a chart is sometimes also referred to as its *interval* or *resolution*.

## Visuals

Plots, colors, fills, backgrounds, levels, bar plotting/coloring, lines/boxes/polylines, labels/text/shapes, tables.

- `visuals/backgrounds.md`: **Backgrounds.** The `bgcolor()` function changes the color of the script's background.
- `visuals/bar_coloring.md`: **Bar coloring.** The `barcolor()` function colors bars on the main chart, regardless of whether the script is running in the main chart pane or a separate pane.
- `visuals/bar_plotting.md`: **Bar plotting.** The `plotcandle()` built-in function is used to plot candles.
- `visuals/colors.md`: **Colors.** Script visuals can play a critical role in the usability of the indicators we write in Pine Script®.
- `visuals/fills.md`: **Fills.** Some of Pine Script's visual outputs, including plots, hlines, lines, boxes, and polylines, allow one to fill the chart space they occupy with colors.
- `visuals/levels.md`: **Levels.** Levels are lines plotted using the `hline()` function.
- `visuals/lines_and_boxes.md`: **Lines and boxes.** Pine Script® facilitates drawing lines, boxes, and other geometric formations from code using the `line`, `box`, and `polyline` types.
- `visuals/overview.md`: **Overview.** Well-designed visuals make indicators and strategies easier to use and less cluttered.
- `visuals/plots.md`: **Plots.** The `plot()` function is the most frequently used function used to display information calculated using Pine scripts.
- `visuals/tables.md`: **Tables.** Tables are objects that can be used to position information in specific and fixed locations in a script's visual space.
- `visuals/text_and_shapes.md`: **Text and shapes.** Pine Script® features five different ways to display text or shapes on the chart:

## Writing scripts

Style guide, debugging, profiling/optimization, limitations, publishing.

- `writing_scripts/debugging.md`: **Debugging.** TradingView’s close integration between the Pine Editor and the Supercharts interface enables efficient, interactive debugging of Pine Script® code.
- `writing_scripts/limitations.md`: **Limitations.** As is mentioned in our Welcome page:
- `writing_scripts/profiling_and_optimization.md`: **Profiling and optimization.** Pine Script® is a cloud-based compiled language geared toward efficient repeated script execution.
- `writing_scripts/publishing_guidelines.md`: **Publishing guidelines (house rules checklist).** Condensed House Rules, visibility types, vendor requirements, and BBCode formatting for script descriptions. Hand-maintained.
- `writing_scripts/publishing.md`: **Publishing scripts.** TradingView hosts a large global community of Pine Script® programmers, and millions of traders.
- `writing_scripts/style_guide.md`: **Style guide.** This style guide provides recommendations on how to name variables and organize your Pine scripts in a standard way that works well.

## FAQ

Task-oriented answers and techniques.

- `faq/alerts.md`: **Alerts.** In indicator scripts, there are two ways to define triggers for alerts:
- `faq/data_structures.md`: **Data structures.** Pine data structures resemble those in other programming languages, with some important differences:
- `faq/functions.md`: **Functions.** Many built-in technical analysis (TA) functions have a `length` parameter, such as `ta.sma(source, length)`.
- `faq/general.md`: **FAQ.** Suppose, we have a Heikin Ashi chart (or Renko, Kagi, PriceBreak etc) and we've added a Pine script on it:
- `faq/indicators.md`: **Indicators.** The Volume and Visible Range Volume Profile indicators (along with some other built-in indicators) are written in Java.
- `faq/other_data_and_timeframes.md`: **Other data and timeframes.** Generally speaking, the `request.security()` function can get the same kinds of data from another timeframe that is available on the chart timeframe.
- `faq/programming.md`: **Programming.** The *scope* of a variable is the part of a script that defines the variable and in which it can be referenced.
- `faq/strategies.md`: **Strategies.** Using Pine Script® strategy scripts, users can test *simulated* trades on historical and realtime data, to backtest and forward test trading systems.
- `faq/strings_and_formatting.md`: **Strings and formatting.** Scripts can display text using the following methods:
- `faq/techniques.md`: **Techniques.** Both these errors occur when creating objects too distant from the current bar.
- `faq/times_dates_and_sessions.md`: **Times, dates, and sessions.** The following example script initializes a variable using the var keyword on the first bar and then never updates it again.
- `faq/variables_and_operators.md`: **Variables and operators.** In Pine Script®, the `close` variable represents the current price.
- `faq/visuals.md`: **Visuals.** In Pine Script®, scripts cannot place `plot()` calls directly within `if` or `for` statements – or in any other local scopes.

## Errors

Compiler/runtime error and warning codes.

- `errors/CE10101.md`: **errors/CE10101.md.** This compilation error occurs if one or more of the *conditions* that control the flow of a conditional structure (an `if` or `switch` statement) returns a value that is *not* of the “bool” type.
- `errors/CE10117.md`: **errors/CE10117.md.** This compilation error indicates that a script’s compiled code is *too large* for the runtime system to execute.
- `errors/CW10003.md`: **errors/CW10003.md.** This compiler warning occurs if a call to a built-in function or user-defined function (or method) inside a conditional structure or loop retrieves data from its calculations on *past bars* by using t
- `errors/overview.md`: **Overview.** Pine Script® uses *runtime errors*, *compilation errors*, and *compiler warnings* to help prevent unintended or erroneous script behaviors:
- `errors/RE10139.md`: **errors/RE10139.md.** The most common cause of this error is the retrieval of custom objects and collections from `request.*()` functions such as `request.security()`.
- `errors/RE10143.md`: **errors/RE10143.md.** In Pine Script®, a single script executes from start to end on each bar of the chart.

## Migration guides

Converting older scripts; `to_pine_version_6.md` lists every v5 to v6 breaking change.

- `migration_guides/overview.md`: **Overview.** The migration guides catalogue all changes introduced between Pine Script® versions and explain how to rewrite code to convert the indicator successfully.
- `migration_guides/to_pine_version_2.md`: **To Pine Script® version 2.** Pine Script version 2 is fully backwards compatible with version 1.
- `migration_guides/to_pine_version_3.md`: **To Pine Script® version 3.** This document helps to migrate Pine Script code from `@version=2` to `@version=3`.
- `migration_guides/to_pine_version_4.md`: **To Pine Script® version 4.** This is a guide to converting Pine Script code from `@version=3` to `@version=4`.
- `migration_guides/to_pine_version_5.md`: **To Pine Script® version 5.** This guide documents the **changes** made to Pine Script from v4 to v5.
- `migration_guides/to_pine_version_6.md`: **To Pine Script® version 6.** Pine Script v6 introduces a number of changes and new features.

## Other

- `release_notes.md`: **Release notes.** Every Pine Script change since 2020, newest first.
