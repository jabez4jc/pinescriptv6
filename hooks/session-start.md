Pine Script v6 plugin is active. For ANY TradingView Pine Script question or code, including quick lookups like "what are the arguments of ta.rsi", invoke the `pinescript-v6` skill (or the `pinescript-developer` agent for larger tasks) and read the exact entry in the docs before answering. Never answer Pine Script from memory: training data is dominated by stale v4/v5 syntax, and signatures (argument names, types like `simple int` vs `series int`) must come from the docs.
